# ── NexuX V9.5 Backend Dependencies ──
# Local-First: No cloud AI SDKs required.
# Smart architecture: auto-captions + partial download = minimal deps needed.

# ── CORE (Required — pipeline runs with just these) ──
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.20
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
yt-dlp>=2026.1
httpx>=0.28

# ── TRANSCRIPTION (Required for whisper fallback) ──
# If YouTube auto-captions available, these are NOT used (fast path)
faster-whisper>=1.1
ctranslate2>=4.5

# ── VISION / FACE DETECTION ──
opencv-python-headless>=4.10,<5.0
mediapipe>=1.0
numpy>=1.26,<3.0

# ── ML BACKEND (Required by faster-whisper) ──
torch>=2.2

# ── SYSTEM MONITORING ──
psutil>=5.9

# ── VOICE-OVER (V8.0 — edge-tts for TTS voice-over) ──
edge-tts>=6.1

# ── TESTING ──
pytest>=8.0
pytest-asyncio>=0.24

# ── OPTIONAL: Advanced diarization (whisper fallback) ──
# whisperx is imported lazily — install only if needed:
# pip install whisperx
# openai-whisper is imported lazily — install only if needed:
# pip install openai
