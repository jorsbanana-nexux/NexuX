# Fronted Production UI

This directory is the canonical NexuX frontend. The previous NexuX frontend is no longer the primary product surface.

The UI is intentionally kept inside NexuX so the engine and product interface ship as one deployable system.
