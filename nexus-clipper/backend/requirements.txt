# ── NexuX V7.0 Backend Dependencies ──
# Local-First: No cloud AI SDKs required.
# Only direct dependencies are listed; pip resolves transitive deps.

# ── API Server ──
fastapi>=0.141,<1.0
uvicorn[standard]>=0.52,<1.0
websockets>=16.0
python-multipart>=0.0.30
pydantic>=2.13,<3.0
pydantic-settings>=2.15,<3.0

# ── Video Download ──
yt-dlp>=2026.7

# ── FFmpeg ──
ffmpeg-python>=0.2

# ── Local Transcription (Whisper) ──
faster-whisper>=1.2
ctranslate2>=4.8

# ── Vision / Face Detection ──
opencv-python-headless>=5.0
mediapipe>=1.0
numpy>=2.0
scipy>=1.18
scikit-image>=0.26
pillow>=12.0

# ── ML Backend ──
torch>=2.13
torchaudio>=2.11

# ── Video Processing ──
moviepy>=2.2

# ── HTTP Client (webhooks) ──
httpx>=0.28

# ── Config ──
python-dotenv>=1.2

# ── Testing ──
pytest>=9.0
